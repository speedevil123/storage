import React from 'react';
import { Modal } from 'antd';

const Statistics = ({ visible, onConfirm, onCancel }) => {
    return (
        <h1 style={{textAlign: 'center'}}>Statistics</h1>
    );
};

export default Statistics;
