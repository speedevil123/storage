import React from 'react';
import { Modal } from 'antd';

const Contacts = ({ visible, onConfirm, onCancel }) => {
    return (
        <h1 style={{textAlign: 'center'}}>Contacts</h1>
    );
};

export default Contacts;
